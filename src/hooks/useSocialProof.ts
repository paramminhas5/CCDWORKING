/**
 * Social proof hooks — stub versions.
 * Return null/0 when Supabase isn't connected. Can be wired to real counts later.
 */

export function useSignupCount(): number | null {
  return null;
}

export function useEventRsvpCount(_slug: string): number | null {
  return null;
}

export function usePlatformStats() {
  return null;
}
